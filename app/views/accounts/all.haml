%table{:class => "table table-hover"}
	%tr
		%th
			Name
		%th
			ID
	-Account.all.each do |account|
		%tr{:id => :user, :onclick => "window.location='#{PadrApp.url(:accounts,:view,:id => account.id)}';"}
			%td
				=link_to account.name, PadrApp.url(:accounts,:view,:id => account.id)
			%td
				account.id
	%br
