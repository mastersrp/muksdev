%h3=link_to @account[:name], url(:accounts, :view, :id => @account[:_id])
%img{:src => "http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(Account.get(@account[:_id]).email.downcase)}?s=64?r=pg"}
-if @account.has_permission?("admin") then
	%span{:class => "label label-important"}
		Admin
