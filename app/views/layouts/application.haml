!!!
%html
	%head
		%meta{:content => "text/html; charset=utf-8", "http-equiv" => "Content-Type"}
		=stylesheet_link_tag 'aloha', 'desktop'
		=javascript_include_tag 'application', 'jquery', 'require', 'aloha'
		%title
			PadrApp
	%body
		#header
		.flash=[:error,:warning,:notice].map { |type| flash_tag(type,:class => "message #{type}") }.join
		=partial("base/sidebar")
		#content
			=yield
		#footer
			%p
				Copyright &copy;
				=Time.now.year
				%a{:href => 'http://necrophcodr.co.cc'} necrohpcodr
				
		:javascript
			Aloha.ready(function(){
				Aloha.jQuery("#editable").aloha();
			});
			var changeApp = function(){
				// var option = $("input#changeApp").attr("selected")
			}
